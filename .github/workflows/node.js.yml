# This workflow will do a clean install of node dependencies, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Node.js CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      env:
        NEXT_PUBLIC_FIRE_KEY: AIzaSyAYTFtql07Xba0XDGOHgQnvQhCbo8VcHiw
        NEXT_PUBLIC_FIRE_PROJECT_NAME: trainez-a0455
        NEXT_PUBLIC_FIRE_APP_ID: 1:131093028525:web:84d75
        NEXT_PUBLIC_FIRE_MESSAGE_ID: 131093028525
        NEXT_PUBLIC_FIRE_MEASUREMENT_ID: G-KSYR4ZE3PD
        NEXT_PUBLIC_STREAM_APP_ID: 102445
        NEXT_PUBLIC_STREAM_KEY: et996ub2qf5f
        STREAM_SECRET: ${{ secrets.STREAM_SECRET }}
        STRIPE_SECRET: ${{ secrets.STRIPE_SECRET }}
        STRIPE_WEBHOOK_SECRET: ${{ secrets.STRIPE_WEBHOOK_SECRET }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm ci
    - run: npm run build --if-present
    - run: npm test
